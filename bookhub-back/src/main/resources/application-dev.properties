    # ============================================================
#  Profil DEV — ne jamais utiliser en production
# ============================================================

# Base de données (locale)
dbname=bookhub
spring.datasource.url=jdbc:sqlserver://localhost;database=${dbname};integratedSecurity=false;encrypt=false;trustServerCertificate=true
spring.datasource.username=sa
spring.datasource.password=Pa$$w0rd

# JWT — secret fixe pour le dev
jwt.secret=BBgA$Yo_ffKvQlKsaxLM}1=RlP:uDt+Z&I:%uj!$90&

# SQL — logs verbeux pour le développement
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
# Affiche les valeurs des paramètres liés ("?" dans les requêtes)
logging.level.org.hibernate.orm.jdbc.bind=trace
logging.level.org.springframework.data=DEBUG

# Logs applicatifs
logging.level.root=INFO
logging.level.fr.eni.bookhub=DEBUG

# Swagger activé en dev
springdoc.swagger-ui.enabled=true
springdoc.swagger-ui.path=/swagger-ui.html
